echo "Running pre-push checks..."

# Run type check
echo "→ Type checking..."
pnpm type-check || exit 1

# Run Next.js build
echo "→ Building..."
pnpm build || exit 1

echo "✓ Pre-push checks passed!"
